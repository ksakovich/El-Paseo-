import React from 'react';

const cart = () => (
    <div>
        <h1>This will be a CART Page!!!</h1>
        <button onClick={() => { console.log('Cart Clicked'); }}> Click</button>
    </div>
);
export default cart;